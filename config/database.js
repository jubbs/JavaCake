module.exports = {
  // Database host
  host: 'localhost',

  // Database user
  user: 'root',

  // Database password
  password: '',

  // Database name
  database: 'javacake_db',

  // Connection pool limit
  connectionLimit: 10,

  // Wait for connections
  waitForConnections: true,

  // Queue limit
  queueLimit: 0
};
